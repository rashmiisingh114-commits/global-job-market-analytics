# Core data analysis
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # Parquet read/write

# Statistics
scipy>=1.11
statsmodels>=0.14

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Machine learning
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# Notebook environment
jupyter>=1.0
notebook>=7.0
nbconvert>=7.0
nbformat>=5.9

# SQL connectivity (MySQL)
SQLAlchemy>=2.0
PyMySQL>=1.1

# Note: the original project brief also listed Plotly and Power BI in the tech stack.
# Plotly was not ultimately used (Matplotlib/Seaborn covered all static visualization needs);
# Power BI is a separate desktop application, not a Python dependency — see docs/POWER_BI_DASHBOARD_SPEC.md.
